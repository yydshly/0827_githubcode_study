# Web Demo Validation

验证时间：2026-08-28 18:55（Asia/Shanghai）  
规范入口：`http://127.0.0.1:4174`  
结果：**PASS**

## 自动化矩阵

| 表面 | 主题 / 偏好 | 结果 |
| --- | --- | --- |
| 1440 × 1000 desktop | light | HTTP 200；无空白、无错误覆盖层、无 console error、无横向溢出 |
| 768 × 1024 tablet | dark | 同上；暗色语义层级保持 |
| 390 × 844 mobile | light | 同上；单列主旅程与按钮可达 |
| 1024 × 768 | reduced motion | 媒体偏好命中，内容完整 |

浏览器还验证了：Stage 1.5 原候选 `reject-as-standalone`、修订候选 `confirmed-for-stage2`、18 案例总数、Stage 2 `static-pass`、RIA++ 六段与五种路由、Stage 4 `behavior-pass`、最终 19 / 19 和负例 0 误触发、Stage 5A `repo-install-pass` 与三类宿主证据、Stage 5B 两份官方来源/五模式/八段完整审计/三类证据账本/六条因果/六项行动合同/86 分结论、247 总量与五类计数，以及全部既有搜索、对话框、键盘、主题、状态恢复和错误态交互。

Revision 2 新增验收：

- 空问题显示错误、设置 `aria-invalid` 并把焦点送回输入框；
- 键盘可以载入示例并运行候选能力；
- 学习、故障、架构与领导力四类问题全部路由正确；
- 每次结果包含 4 个执行阶段、4 条候选证据、交付清单和停止边界；
- 从试跑结果打开证据详情后，Escape 关闭并回到原证据按钮；
- 运行后桌面与 390px 手机结果均无横向溢出或 console error。

Revision 3 新增验收：

- 学习、故障、架构和领导力四条技术路线均被识别为核心域，并保留 4 阶段、4 证据的完整候选执行链；
- “我想学习摄影，应该怎样制定三个月的实践计划？”被识别为相邻域，显示“方法迁移待验证”且继续标注验证边界；
- 股票投资问题被识别为域外，显示停止理由和建议来源，结果中为 0 个执行阶段、0 条候选证据；
- 域外结果修改为线上故障问题后恢复核心域，旧停止状态不残留；
- 三态流程通过键盘、桌面和移动端验收，无横向溢出或 console error。

Revision 4 新增验收：

- 首屏与流水线显示 `1 / 247 pilot`，没有把一个试点误写成全量 Stage 1.5 完成；
- 原“线上故障复盘”显示 V3 未通过和 `reject-as-standalone`；
- 修订后的“系统性故障学习审查”显示 `provisional-pass`，并明确仍等待用户确认；
- V1、V2、V3 三张验证卡完整，8/3/2/2/3 五类测试分布合计为 18；
- 故障能力试跑保留 Stage 1 沙盒，同时显示 Stage 1.5 修订提示；
- 新增试点区在 1440 浅色、768 深色和 390 浅色下可读，无横向溢出或 console error；
- 规范入口改为 4174；4173 被本机另一个项目占用，没有终止或修改该无关服务。

Revision 5 新增验收：

- 首屏与流水线显示 `1 Skill / v0.1.0`，Stage 1.5 修订候选状态推进为 `confirmed-for-stage2`；
- Stage 2 构造区显示 `incident-learning-audit`、v0.1.0 与 `static-pass`；
- 六张 RIA++ 卡、五种运行路由、四个构造产物和两个“按需参考”完整；
- 页面明确 Stage 4 独立 Agent 盲测尚未执行，Skill 没有安装；
- 故障路线名称对齐“系统性故障学习审查”，但继续标记为确定性协议演示；
- 1440 浅色、768 深色、390 浅色与 reduced-motion 均无横向溢出或 console error；
- 人工视觉复核修正了桌面专用截图的粘性页头遮挡，最终桌面和手机证据层级清晰。

Revision 6 新增验收：

- 首屏和流水线显示 `Stage 4 · v0.1.1 · 19 / 19`、`1 Skill / v0.1.1` 与 `19 / 19 blind`；
- Stage 2 构造区继续单独证明六段、八步、五路、两参考和静态校验，不把行为证据倒写进构造证明；
- Stage 4 区显示 v0.1.0 首轮 16 / 19、v0.1.1 聚焦复测 3 / 3 和最终全量回归 19 / 19；
- 三个失败—修订卡完整，最终矩阵为 8/8 应触发、5/5 边界、6/6 不应触发和 0 误触发；
- 盲态协议明确隐藏题型、期望行为、评分标准、前轮结果和其他 Agent 输出，并由主代理独立判分；
- 页面明确 19/19 仅覆盖合成合同，真实组织事故材料尚未评测，Skill 仍未安装；
- 故障能力沙盒的状态提示已对齐 v0.1.1 Stage 4 结果，同时继续明确浏览器没有运行真实 Agent；
- 1440 浅色、768 深色、390 浅色与 reduced-motion 均无横向溢出、console error 或交互回归；
- 人工视觉复核覆盖首屏、完整 Stage 4 桌面区块和完整移动端三列联系表，层级、卡片节奏与诚实边界均清晰。

Revision 7 新增验收：

- 首屏和流水线显示 `Stage 5A · v0.1.1 · repo-installed` 与 `5A · repo-installed`，同时明确完整 Stage 5 未完成；
- Stage 5A 安装主卡显示仓库级范围、`.agents/skills/incident-learning-audit` 与 6 / 6 SHA-256 一致；
- 三张真实宿主证据卡完整：显式发现、事故语义隐式触发和纯架构负例不触发；
- 显式、隐式和活动事故 `active_handoff` 三类调用提示词均可读；
- 价格服务事故示例显示 `full_review`、影响摘要和“尚未形成学习闭环”，键盘 Enter 可展开完整 8 节交付结构；
- 性能观察明确区分短路由输出成功与两次嵌套 CLI 长输出人工中断，不把延迟写成能力或发现失败；
- Stage 2、Stage 4 与 Stage 5A 证据分层文案通过，页面不再保留“仍未安装”的过时状态；
- `npm.cmd run check` 同时执行 Stage 5A 6 文件哈希和宿主证据不变量校验；
- 1440 浅色、768 深色、390 浅色和 reduced-motion 均无横向溢出、console error 或既有交互回归；
- `agent-browser` CLI 在当前 Windows 环境不可用，真实浏览器验证由项目内 Playwright 路径完成；截图和 DOM/交互证据完整。

Revision 8 新增验收：

- 首屏和流水线显示 `Stage 5B · public case`，同时保留完整 Stage 5 未完成的边界；
- 真实案例使用 Cloudflare 2019 WAF 全球中断，并展示两份 Cloudflare 官方来源；
- 五种响应模式可用鼠标、方向键、Home 和 End 切换，允许输出与禁止输出同时可见；
- `full_review` 四项前置检查和八段输出可逐步导航，上一段/下一段会把焦点送至新面板；
- 证据账本区分事实、推断和未知；“未知”筛选精确返回 2 项；
- 六条因果均展示证据强度和可证伪条件；六项整改均展示负责人、期限、验收、失败条件和关闭缺口；
- 结论稳定显示 86 / 100 与“尚未形成学习闭环”，不把计划或单项后续证据冒充全部关闭；
- Stage 5B 结构校验已并入 `npm.cmd run check`，真实 Chromium 全矩阵通过且没有既有交互回归；
- 新增桌面与手机 Stage 5B 专用截图，结构化证据写入 `validation.json`；
- Revision 9 通过 Node 图像通道完成模型侧像素复核；Stage 5B 的 DOM、截图、键盘交互与多视口证据继续有效。

Revision 9 新增验收：

- 首屏主叙事改为 4 项能力方向、1 个正式 Skill 与 3 个候选能力，CTA 直达能力资产和最佳演示；
- 四张能力卡均显示适用问题、结果、3 项输入、4 项交付、2 条质量门和停止边界；
- 六级成熟度严格显示事故审查 6 / 6、其余三项 1 / 6，不把候选方向冒充正式 Skill；
- 正式能力卡进入 Cloudflare 真实案例，候选能力卡会载入相应确定性沙盒试跑；
- 最佳演示新增输入—路由—证据—因果—行动—闭环六站，以及 `full_review`、3 类证据、6 层原因、6 项行动、86 / 100 快照；
- 六站按钮映射到正确输出，并在异步滚动后把焦点送到路由控件或审计面板；
- 验收发现并修复学习示例同时触发架构关键词的歧义；修正后学习卡稳定进入“技术学习规划”；
- 1440 浅色、768 深色、390 浅色和 reduced-motion 专测均为 4 卡、1 正式、3 候选、无溢出、无 console error；
- 人工视觉复核收紧卡片密度、修正最佳演示标题断行和主按钮对比，并清理专用截图中的粘性控件；
- 独立结构化证据写入 `showcase-validation.json`，标准 `verify:browser` 会串行执行既有回归和 Revision 9 专测。

Stage 5C 接入门新增验收：

- 授权、脱敏、密钥移除、事故稳定性、时间线、证据引用、Skill 版本与独立复核已形成机器可检合同；
- 当前授权组织案例为 0，校验结果明确为 `intake-ready-awaiting-authorized-cases`，组织外部效度和完整 Stage 5 均保持 false；
- 最佳演示在四种浏览器表面显示 Stage 5C 下一质量门，不把“接入基础就绪”写成“组织认证通过”；
- 完整项目检查同时运行 Stage 5A、Stage 5B 和 Stage 5C 三层校验。

Revision 10 新增验收：

- 首屏定位为“高价值长内容到 Agent Skills 的方法工厂”，CTA 依次进入库能力与代表案例；
- 库能力区稳定渲染 6 项核心能力、8 类来源、7 阶段、3 方责任边界和 2 类适配判断；
- 案例画廊稳定渲染 5 个上游代表案例与 1 个本地受控案例，每张卡都展示规模、证明力、证据口径和局限；
- 全部、视频与课程、资料与工件集、长期文档、我们的验证五个筛选分别返回 6 / 2 / 2 / 1 / 1，鼠标和键盘都可操作，`aria-pressed` 与实时计数同步；
- 《左耳听风》明确标注为 119 篇代理语料的受控研究，并可继续进入 4 项能力资产与 Stage 5B 完整 Skill 演示；
- `library-catalog.json` 由独立脚本校验数量、唯一 ID、阶段序列、链接、证据和边界字段；
- 1440 浅色、768 深色、390 浅色与 1024 reduced-motion 全部无横向溢出、console error 或既有功能回归；
- 独立结构化证据写入 `library-validation.json`，标准 `verify:browser` 现在串行执行三层真实浏览器验证。

Revision 11 新增验收：

- 首屏第二入口改为“对照同类产品”，顶部导航可以直接进入生态对照区；
- 生态价值链稳定渲染 6 层：来源解析、能力蒸馏、构造治理、行为评测、标准参考和发现分发；
- 生态目录稳定渲染 9 个外部项目，每张卡都有来源形态、最强项、不能替代项、采用价值、核对口径、风险与官方链接；
- 全部、直接同类、质量与参考、标准与分发四个筛选分别返回 9 / 5 / 2 / 2，鼠标和键盘操作均同步 `aria-pressed` 与实时计数；
- 五层采用建议稳定显示 Skill Seekers、Cangjie Skill、skill-factory、agent-skill-eval、Agent Skills + skills.sh，并明确除 Cangjie 外均为建议研究或尚未集成；
- 结构校验要求 6 层、9 项、5 层推荐、唯一 ID、官方 HTTPS 域名、比较字段和风险字段完整；
- 视觉复核发现并修复生态筛选选中态使用未定义前景色变量导致标签不可见的问题；修复后浅色、深色、390px 与 reduced-motion 均通过；
- 原 6 项库能力、8 类来源、6 个代表案例、4 项本地能力资产与 Stage 5B 完整演示无回归。

Revision 12 新增验收：

- 页面新增统一研究地图，精确关联 13 个既有区段：库能力、生态、代表案例、本地案例、能力资产、流水线、Stage 1.5、Stage 2、Stage 4、Stage 5A、Stage 5B、候选证据和能力试用；
- 报告中心关联 7 份远端 GitHub 文档：研究总览、《左耳听风》实验报告、库能力说明、生态对照、上游案例目录、验证记录与发布复现；
- 顶部导航和首屏主按钮均可进入研究地图，键盘 Tab 路径可见并正确落到目标区段；
- 页面、根 README 与研究总览三处均明确当前案例基于 119 篇第三方公开专栏代理文本，不是纸质书全文数字化或逐章审查；
- 1440 浅色、768 深色、390 浅色与 1024 reduced-motion 四个表面均稳定显示 13 个锚点、7 个报告和边界提示，无横向溢出或 console error；
- `npm.cmd run check`、基础回归、Revision 9 展示专测与 Revision 10–12 库级专测全部通过，既有库、生态、案例、能力资产、Stage 与试用台无回归；
- 人工复核桌面与移动端截图后，将第 13 个奇数卡片调整为整行，消除栅格空洞并保持移动端自然堆叠。

结构化结果见 [validation.json](../evidence/validation.json)、[showcase-validation.json](../evidence/showcase-validation.json) 与 [library-validation.json](../evidence/library-validation.json)。

## 本地截图

浏览器回归会在 `evidence/` 生成各视口、主题和关键流程截图。截图属于可重建的本地验收产物，体积较大，不进入远端研究发布；结构化验证 JSON 保留在仓库中，线上页面本身作为最终可视结果。

## 可复跑命令

~~~powershell
npm.cmd run check
npm.cmd start
# 另一个终端
npm.cmd run verify:browser
~~~

浏览器脚本优先使用项目内 `playwright`；在 Codex 桌面环境中会回退到随附的 Playwright 运行时。错误态测试会主动请求 `data/missing.json`，该 404 是预期测试输入。
